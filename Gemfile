source "https://rubygems.org"

# This locks Jekyll to the version GitHub Pages uses
gem "github-pages", group: :jekyll_plugins

# You can add a few whitelisted plugins like this:
group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jemoji'
end
